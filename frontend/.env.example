# Base URL of the Vitamin Advisor backend API (read at build time by Vite).
# Local dev: leave unset to default to http://localhost:8001
# Vercel: set to the Railway backend URL, e.g. https://vitamin-advisor.up.railway.app
VITE_API_BASE_URL=
